import { TextInputRendrer } from './text-input-rendrer';

describe('TextInputRendrer', () => {
  it('should create an instance', () => {
    expect(new TextInputRendrer()).toBeTruthy();
  });
});
